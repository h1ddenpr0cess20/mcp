use_default_settings: true

general:
  instance_name: "web-mcp searxng"
  debug: false

server:
  port: 8888
  bind_address: "127.0.0.1"
  secret_key: "web-mcp-searxng-local-dev"
  limiter: false
  image_proxy: true
  method: POST

search:
  safe_search: 0
  default_lang: "en"
  formats:
    - html
    - json
